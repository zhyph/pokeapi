import React from 'react'

function Pokemons() {
    return (
        <div>
            {/* <ul>
        {poke.map((item, id) => (
          <li
            key={id}
            style={{ listStyle: "none", textIndent: "40px", marginLeft: "40%" }}
          >
            {item.name[0].toUpperCase() + item.name.substring(1)}
          </li>
        ))}
      </ul> */}
        </div>
    )
}

export default Pokemons
